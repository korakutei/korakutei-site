# 交樂庭（KORAKUTEI）コーポレートサイト 引継ぎメモ

このファイルは Claude Code に読み込ませて作業を引き継ぐための資料です。
プロジェクトフォルダのルートに `CLAUDE.md` として置いてください。

## 1. プロジェクト概要

- **会社名**：合同会社交樂庭（Ko Raku Tei）
- **代表社員**：浦野雄次（うらのゆうじ）
- **連絡先**：korakutei29@gmail.com
- **サイトの目的**：ブランドサイト（一般公開）。事業内容・実績・慶元寺ベニュー紹介・代表者紹介を掲載
- **Vision**：IKIGAIに出逢える機会を提供する
- **Mission**：ワクワクする交流機会を創造する
- **事業領域（3本柱）**：
  1. 賑わい創出事業（地域交流活性化・イベント企画運営）
  2. EQ支援事業（けん玉等を通じた ～ココリンク～）
  3. コンテンツ開発支援事業（地域コンテンツ・PR写真動画・特殊撮影）

※「合同会社交樂庭」と「地域未来にぎわい工房」（別プロジェクト・JTB×Airbnb共創）は別組織・別事業なので混同しないこと。

## 2. ファイル構成

- **メインファイル**：`korakutei.html`（単一HTMLファイル、外部依存なし）
- 写真はすべて **Base64で埋め込み済み**（`<img src="data:image/jpeg;base64,...">`）
- 現在のファイルサイズ：**約1.7MB**
- CSS・JSも同一ファイル内に記述（`<style>` 1ブロック、末尾に軽量な `<script>`）

## 3. デザインシステム

### カラーパレット（`:root` の CSS変数）
```css
--bg: #F6F1E6;           /* 和紙クリーム（背景） */
--bg-deep: #EFE7D6;      /* フッター等の深めのクリーム */
--ink: #1B2A41;          /* 藍色（本文・見出し） */
--ink-soft: #3d4c63;     /* 補助テキスト */
--vermillion: #BE3B26;   /* 朱印レッド（アクセント・縁スタンプ） */
--vermillion-deep: #9c2f1e;
--moss: #4B5C43;         /* 寺境内の緑 */
--gold: #A6813C;         /* 金箔アクセント */
--paper-line: rgba(27,42,65,0.12); /* 罫線・区切り */
```

### フォント
- 見出し：Shippori Mincho（`.mincho`, h1/h2/h3）
- 本文：Noto Sans JP
- Google Fonts経由で読み込み（`<head>` 内 `<link>` 参照）

### 和柄モチーフ（2026/08/22 追加）
テーマは「和・輪・縁・宴・延」＝円のモチーフ。
- **全体背景**：七宝つなぎ（重なる円）を極薄グレーで敷いている（body の `background-image`、SVGをdata URIで直接埋め込み）
- **見出し下の装飾**：`.sec-head h2::after` に3つの小さな輪が連なるライン（朱色。VMVセクションのみ金色に切替）
- **VMV（紺の理念セクション）**：金色の青海波（せいがいは）を薄く重ねている
- **フッター**：同じ七宝つなぎを朱色で薄く
- 柄を変更・追加する場合はこの4箇所の `background-image` の SVG data URI を編集すればよい（他のライブラリ依存なし）

### 「縁」スタンプモチーフ
既存の実績カウント（`.stamp-mark`）に朱色の「縁」の印章SVGを使用。これがブランドのシグネチャ要素なので流用・拡張する際は踏襲すること。

## 4. ページ構成（セクションID）

| id | 内容 |
|---|---|
| `hero` (id無し、`.hero`) | ロゴ・キャッチコピー |
| `about` | 交樂庭とは（想いの背景） |
| `vmv` | Vision/Mission/Value（紺背景） |
| `capital` | 「縁的資本という希望」5段階循環図＋副住職の声（匿名） |
| `business` | 事業領域3カード（賑わい創出／EQ支援／コンテンツ開発、各写真付き） |
| `track` | 実績・事例（縁カウント001〜005、写真付き） |
| `venue` | 慶元寺ユニークベニュー紹介＋オプション体験4種（写真付き） |
| `rep` | 代表者紹介（浦野雄次） |
| `footer` | 締めの言葉・メールアドレス |

## 5. 使用している実写真（PDF資料から抽出済み）

会社概要PDF（`0707_交樂庭事業概要.pdf`）から `pdfimages` で抽出し、リサイズ・圧縮（quality 62前後）してBase64埋め込み。抽出済みファイルはこの引継ぎには含まれていないため、**追加で写真を差し替えたい場合は元のPDF/写真を再度アップロードしてもらう必要がある**。

現在使用中の写真（用途）：
- ロゴ（和柄円形）→ ヘッダー・ヒーロー
- 浦野家の庭 → 「交樂庭とは」
- 慶元寺山門と桜 → ベニュー紹介
- 慶元寺での太鼓演舞 → インバウンド実績
- 浦野雄次（着物・腕組み、2026/08/22差し替え済み最新カット）→ 代表者紹介
- 喜多見商店街モノクロ → 「ありたい姿」背景
- 寺ヨガ・御神輿、子ども向けEQプログラム、ドローン撮影 → 事業領域3カード
- みたままつり提灯、金沢の伝統家屋 → 実績セクション
- 慶元寺本堂内観2枚 → ベニューギャラリー
- 写仏・念仏木魚・鮨・ロケーションフォト → オプション体験4カード

## 6. マスタールール（浦野様の指示、コンテンツ生成時に適用）

- 日本語で回答
- 「合同会社交樂庭」と「地域未来にぎわい工房」は混同しない
- 確定事業と構想段階のアイデアは明確に分ける
- 個人情報（副住職の実名など）は原則匿名化。実名掲載には浦野様の確認が必要
- 情報の基準日は2026年8月9日

## 7. 既知のトラブル対応履歴

- 画像「？」表示問題 → 全画像をBase64でHTMLに埋め込み済み（外部ファイル参照なし）
- スクロール時に要素が表示されない問題 → IntersectionObserverによるフェードイン演出は廃止済み（`.reveal` は常に `opacity:1`）
- ファイルサイズ超過問題 → 画像を quality:62 程度に再圧縮して対応。**今後さらに写真を追加する場合はサイズに注意**（現在1.7MB、3〜4MBを超えるとアクセスしづらいユーザーが出る可能性）

## 8. 未確認・今後の相談事項

- 実写真のさらなる追加・差し替え（慶元寺内部の別カット、ヨガの様子など）
- コンタクトフォームの要否
- 「交樂庭が選ばれる理由」（差別化ロジック）セクションの追加要否
- 複数ページ構成への拡張要否（現状は1枚のLPスタイル）

## 9. Claude Codeで作業する際の注意

- このファイルは単一HTMLなので、`git init` してからバージョン管理を始めるのがおすすめ
- Base64画像が大半を占めるため、`git diff` は見づらくなる。画像を外部ファイル化してGit管理する運用に切り替えることも検討可（その場合はサーバーが必要になる点に注意）
- CSS/HTML/JSが1ファイルに混在しているため、規模が大きくなってきたら分割（`style.css`, `main.js` 等）も検討
