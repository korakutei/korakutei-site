# 交樂庭（KORAKUTEI）コーポレートサイト 引継ぎメモ

このファイルは Claude Code に読み込ませて作業を引き継ぐための資料です。
プロジェクトフォルダのルートに `CLAUDE.md` として置いてください。

## 1. プロジェクト概要

- **会社名**：合同会社交樂庭（Ko Raku Tei）
- **代表社員**：浦野雄次（うらのゆうじ）
- **連絡先**：korakutei29@gmail.com
- **公開URL**：https://korakutei.github.io/korakutei-site/
- **サイトの目的**：自治体・地域事業者・企業からの「相談」「仕事依頼」につながるブランドサイト
- **Vision**：IKIGAIに出逢える機会を提供する
- **Mission**：ワクワクする交流機会を創造する

### 事業定義（2026/09/21 再定義）

> 交樂庭は、**地域資源を見つけ、別の価値へ編集し、小さな商いとして実装する会社**。

- ブランドコピー：**丁寧に縁をつなぎ、賑わいを創る。**
- 事業コピー：**地域にあるものを、新しい価値へ。**

### 事業領域（4領域）

| # | 英名 | 和名 | 内容 |
|---|---|---|---|
| 01 | LOCAL EDITING | 地域資源編集 | 当たり前になっているものを見つけ、新しい意味・役割へ編集する |
| 02 | EXPERIENCE & CONTENT | 体験・コンテンツ開発 | 見るもの→参加するもの／文化→商品へ変換する |
| 03 | LOCAL COMMERCE & MEDIA | 地域商社・メディア | 集める→編集→発信→人を動かす→商流をつくる（代表例：ヨコチョナビ） |
| 04 | PLACE & LAB | 場所を持ち、試す | 自ら場所を持ち、喜多見で企画→実験→商いを繰り返す |

## 2. ⚠️ 最重要：JTB「地域未来にぎわい工房」との棲み分け

「合同会社交樂庭」と「地域未来にぎわい工房」（別プロジェクト・JTB×Airbnb）は別組織・別事業です。
**サイト上で事業表現が重ならないようにすること。**

### 交樂庭のサイトで主語として使わない表現

地域共創プラットフォーム／自治体伴走／企業と地域をつなぐ／地域課題解決／官民連携／
社会実装／地域プロジェクトPMO／地域課題の整理／共創ネットワーク／全国展開／横展開

※「共創」という語自体が禁止ではないが、**サービス名称・事業定義には使わない**。
　2026/09/21時点で index.html 内の該当語は 0 件（「全国」「展開」も 0 件）。文言を足すときは必ず再確認すること。

### 「横展開」に見せない

喜多見（LAB）での実践は「モデルを他地域へ持っていく」ではなく、
**「ここで得た知恵を、それぞれの地域に合わせて、もう一度編集する」** と表現する。
地域ごとに資源を見つけ直し、再編集する会社であることを崩さない。

### 映像制作会社に見せない

ブランドムービー（`#movie`）は「OUR STORY／地域の物語を、伝わるカタチへ。」として、
**地域を編集する手段のひとつ**の位置づけに留める。多言語制作の機能説明・仕様・提供価値はトップに書かない。

### 代わりに使う表現

地域資源を見つける／編集する／組み合わせる／新しい役割をつくる／体験にする／商品にする／
コンテンツにする／メディアにする／場所を活かす／小さく試す／自分たちで運営する／
商いにする／地域に残す／縁を育てる

## 3. ファイル構成

```
index.html              # 単一ページ（全セクション）
assets/css/style.css    # 全スタイル（1ファイル）
assets/js/main.js       # ヘッダー・ナビ・reveal・フォーム・YouTubeファサード
assets/js/movie.js      # 多言語ブランドムービー（YouTube動画IDはファイル冒頭で設定）
assets/js/marie.js      # 看板犬マリーの道案内（右下についてくる・おさんぽマップ）→ 7章
assets/img/             # 写真（jpg + webp の2本立て。<picture>で出し分け）
                        # マリーだけは透過が必要なので png + webp（marie-*）
.claude/launch.json     # ローカルプレビュー用（npx http-server, :4173）
```

- 画像は外部ファイル参照（過去のBase64埋め込みは廃止済み）
- `out/` `動画/` `写真/` `キャラクター/` `*.zip` などの素材は .gitignore 済み

## 4. ページ構成（2026/09/21 再構成）

| 順 | id | 見出し | 内容 |
|---|---|---|---|
| 01 | `.hero` | 地域にあるものを、新しい価値へ。 | 事業コピー＋ブランドコピー＋CTA2つ（マリーは出さない） |
| 02 | `about` | 地域には、まだ名前のついていない価値がある。 | WHAT WE FIND。資源タイル14枚（すべて写真＋一語）＋原点の折りたたみ |
| 03 | `service` | 見つけたものを、商いのかたちに。 | WHAT WE CREATE。4領域カード＋ヨコチョナビ＋けん玉 |
| – | `movie` | 地域の物語を、伝わるカタチへ。 | OUR STORY。PCは文章＝主／映像＝従の2カラム。動画ID未設定なら自動で非表示 |
| 04 | `method` | 地域にあるものから、商いが生まれるまで。 | KORAKUTEI METHOD 5ステップ。中心に「縁的資本」 |
| 05 | `philosophy` | 縁が増えるほど、商いも賑わいも育っていく。 | 紺地。縁的資本の説明＋縁がつながる図＋VMV＋副住職の声 |
| 06 | `project` | 見つけて、編集して、かたちにしたもの。 | 実績5件。全カード「見つけた／編集した／カタチにした／変わった」で統一（METHODの語彙に揃える） |
| 07 | `lab` | 東京・喜多見を、地域の実験場に。 | 掛け合わせテーマ7つ＋LAB相関図＋締め「ここで得た知恵を、それぞれの地域に合わせて、もう一度編集する。」＋慶元寺ベニュー＋オプション4種 |
| – | `vision` | 喜多見で目指していること。 | KITAMI VISION（暗色）。目指すまちの姿 → 呼び名「Learning City ～遊育～」→ 意味「遊びから、学びが生まれるまち。」の順 |
| 08 | `company` | 地域を編集する人間が、現場に立つ。 | 代表紹介＋看板犬マリーのプロフィール（`#marie`）＋会社概要 |
| 09 | `consult` | こんな相談から、はじまっています。 | 相談例（地域・自治体・地域事業者／企業の2列） |
| 10 | `contact` | まだ企画になっていなくても、大丈夫です。 | 相談入口としてのフォーム。PCではフォームの横でマリーが待つ |

グローバルナビ：**ABOUT / SERVICE / PROJECT / LAB / JOURNAL / CONTACT**
（JOURNAL は note.com への外部リンク、CONTACT のみ朱色のボタン）

## 5. デザインシステム

### カラーパレット（`:root` の CSS変数）
```css
--bg:#F6F1E6;        /* 生成り（背景） */
--bg-deep:#EFE7D6;   /* 一段深いクリーム（LAB・会社概要） */
--bg-tint:#F2ECDE;   /* 明度リズム用（SERVICE・相談例） */
--bg-card:#FBF8F1;   /* カード地 */
--ink:#1B2A41;       /* 墨紺（本文・見出し・暗色セクション地） */
--ink-soft:#3d4c63;  /* 補助テキスト */
--vermillion:#BE3B26;/* 朱（アクセント・縁スタンプ） */
--moss:#4B5C43;      /* 苔色（補助ラベル） */
--gold:#A6813C;      /* 金（暗色セクションのアクセント） */
```
派手な色は増やさない。写真と余白が主役。

### フォント
- 見出し・引用：Shippori Mincho（`.mincho`, h1〜h3）
- 本文：Noto Sans JP
- 和文の行組みは `word-break:auto-phrase` で文節改行（対応ブラウザのみ）
- 改行の出し分け：`<br class="br-sp">` はスマホ（560px以下）だけ、`<br class="br-pc">` はそれ以外だけ改行

### 和柄モチーフ（円＝和・輪・縁・宴・延）
- 全体背景：七宝つなぎ（body の background-image）
- 見出し下：3つ以上の輪が連なるライン（`.sec-head h2::after`。暗色セクションは金）
- 暗色セクション（`.philo` `.movie-sec`）：金の青海波
- METHOD の5つの円：青海波／七宝／麻の葉／矢絣／亀甲（`.wagara-*`）

### 「縁」スタンプ
`.stamp-mark` の朱色「縁」印章SVGがブランドのシグネチャ。PROJECTカードの頭に必ず捺す。

### アニメーション（過剰にしない）
- セクション：`.reveal` → IntersectionObserver で `in-view`（main.js）
- 見出し下の線：スクロールで左から伸びる
- カード：`.stagger` を付けた親の直下要素が順にフェードイン
- 写真：PROJECTカードの写真だけ、文字より少し遅れて出現
- 線でつながる演出：METHOD の円環矢印／PHILOSOPHY の「人・場所・文化…→縁的資本」
- `prefers-reduced-motion` で全て無効化

## 6. 写真の方針

**人 × 場所** が伝わる写真を優先。建物単体・観光写真は補助。
「地域を見る会社」ではなく「地域の中で何かを起こす会社」に見せる。

現在の主な使い分け：

| ファイル | 用途 |
|---|---|
| `track-matsuri` | みたままつり。FINDタイル「祭り」／SERVICE 01／PROJECT筆頭 |
| `track-yoga` | 本堂ヨガ。PROJECT |
| `track-taiko` | 太鼓体験。FINDタイル「人」／PROJECT（海外企業視察） |
| `opt-nenbutsu` | 念仏・木魚体験。SERVICE 02／LABオプション |
| `opt-shabutsu` `opt-sushi` `opt-location` | FINDタイル／LABオプション |
| `biz-eq` | 子どものけん玉。FINDタイル「遊び」／LAB相関図 |
| `vision-town` | 喜多見商店街。FINDタイル「商店」／SERVICE 03／LAB相関図 |
| `biz-floral` | 喜多見フローラル。SERVICE 04／LAB相関図 |
| `about-garden` | 浦野家の庭。FINDタイル「暮らし」／原点の折りたたみ |
| `venue-sanmon` `venue-hondou1/2` | 慶元寺。FINDタイル「場所」／ベニュー紹介 |
| `rep-urano` | 代表紹介 |
| `video-*` `drone-*` `movie-poster` | 動画サムネイル |
| `find-history` `find-temple` `find-akiya` `find-sento` `find-yokocho` `find-community` | FINDタイル「歴史／寺院／空き家／銭湯／横丁／コミュニティ」。**サンプル画像**（2026/09/21差し込み） |

### サンプル画像の扱い

`find-*` の6枚は浦野様が用意したサンプル画像で、喜多見の実写ではない。
**実際の場所・活動の写真が撮れたら優先して差し替える**（index.html にも同旨のコメントあり）。

差し替え手順：元画像を `写真/`（.gitignore 済み）に置き、ImageMagick で 400px 四方の JPG＋WebP に変換して同名で上書きする。

```bash
magick 写真/元画像.png -resize 400x400^ -gravity center -extent 400x400 -strip -colorspace sRGB -interlace Plane -quality 74 assets/img/find-sento.jpg
magick 写真/元画像.png -resize 400x400^ -gravity center -extent 400x400 -strip -colorspace sRGB -quality 74 -define webp:method=6 assets/img/find-sento.webp
```
（正方形でない写真も、中央で400px四方に切り抜かれる）

**未使用**：`biz-nigiwai`（= `track-yoga` と同じ本堂ヨガの別トリミング）、`biz-content`（AI生成の合成イメージ。実写優先方針のため不使用）。削除はしていないので、差し替え候補として使える。

## 7. 看板犬マリー（サイトの道案内役）※2026/09/22 追加

サイト全体を「マリーと歩く、まちのおさんぽ」に見立て、訪問者を案内するキャラクター。
右下に常駐して話しかける、サイト内チャットボット的な道案内として置いている。
**トップ（ヒーロー）にはマリーを出さない**（2026/09/22 浦野様判断。一度ロゴ横に置いたが削除）。

- **設定の元資料**：`キャラクター/キャラクター設計.PNG`（元画像 `犬❶〜❹.PNG` も同じフォルダ。.gitignore 済み）
- **プロフィール**：喜多見で生まれ、浦野家で暮らしていたゴールデンレトリバーの女の子。
  性格は天真爛漫・好奇心旺盛、人が大好きな食いしん坊、ちょっぴりおっちょこちょい。
  特技は「はじめての人ともすぐ仲良くなれる」「まちの“いいところ”を見つける」。
  使命は「喜多見と交樂庭の魅力を犬目線で伝える」。
- **話し方**：一人称は「わたし」、「〜だよ」「〜してね」。セリフでも 2章の禁止表現は使わない。
  マリー自身の体験として事実でないこと（例：祭りに参加した等）は書かない

### 登場する場所（マリーは画面に1匹だけ）

| 場所 | 内容 | 画像 |
|---|---|---|
| 右下（`.marie-guide`） | トップを過ぎると出てきて、各セクションでひとこと話す。最初のひとことでは手をふって自己紹介。押すと「おさんぽマップ」（行き先一覧・いまここ表示・相談ボタン） | `marie-sit`／あいさつ中は `marie-wave`／移動中は `marie-run` |
| WHO WE ARE（`#marie`） | プロフィールカード | `marie-sit` |
| CONTACT | 「おさんぽ、おつかれさま！」とフォームの横（スマホはフォームの上）で待つ | `marie-run` |

### 仕組みとルール（`assets/js/marie.js`）

- セリフは各 `<section>` の **`data-marie` 属性**に書く。文言の修正は index.html だけで済む
- はじめて通りかかったセクションで1回だけ、0.8秒立ち止まってから話し、数秒で消える（流し読み中は話さない）
- 最初のひとことにだけ、自己紹介「はじめまして！ 交樂庭の看板犬、マリーです。」と
  「わたしを押すと、おさんぽマップが開くよ。」を添える
- トップ（ヒーロー）が画面の半分以上を占めている間は出さない
- `data-marie-here` を付けた要素（プロフィール・CONTACTのマリー）が見えている間は、右下のマリーは隠れる。
  CONTACTから下でも隠れる（フォームの送信ボタンに重ねないため）
- 「マリーを休ませる」を押すと肉球ボタンだけになり、localStorage の `korakutei-marie-rest` に記憶する
- 吹き出しは読み上げ対象外（見出しと重複するため）。マップ内に同じひとことを読み上げ用に置いている
- `prefers-reduced-motion` では、登場・跳ねる・足あとの動きをすべて止める
- セクションを増やしたら：`data-marie` を足し、必要ならおさんぽマップ（`.marie-map-list`）にも1行足す

### 画像の作り方

元画像（透過PNG・1254px四方）を、透明な余白を切り詰めて高さ480pxの WebP＋PNG にする。
`犬❹` は `犬❶` とほぼ同じ絵なので未使用。

```bash
magick "キャラクター/犬❷.PNG" -fuzz 2% -trim +repage -resize x480 -strip -quality 84 -define webp:alpha-quality=92 -define webp:method=6 assets/img/marie-sit.webp
magick "キャラクター/犬❷.PNG" -fuzz 2% -trim +repage -resize x480 -strip -define png:compression-level=9 assets/img/marie-sit.png
```
（❶ → `marie-wave`、❷ → `marie-sit`、❸ → `marie-run`）

## 8. マスタールール（浦野様の指示）

- 日本語で回答
- 「合同会社交樂庭」と「地域未来にぎわい工房」は混同しない（→ 2章）
- 確定事業と構想段階のアイデアは明確に分ける
- 個人情報（副住職の実名など）は原則匿名化。実名掲載には浦野様の確認が必要
- EQ支援事業のプログラム名「ココリンク」は**サイトに出さない**（2026/09/20 判断）
- ヨコチョナビは「先行公開中」であることを明示したまま掲載する

## 9. 既知の仕様・トラブル対応履歴

- 問い合わせフォームは `main.js` の `CONTACT_ENDPOINT` が空のあいだ **mailto 送信**。
  Formspree等のURLを入れるとAjax送信に切り替わり、案内文とボタン文言も自動で変わる
- YouTubeはクリックされるまで読み込まないファサード方式（`.video-embed[data-yt]`）
- note.com の `embed.js` は配信終了。iframe単体で動作する
- ナビに外部リンク（JOURNAL）があるため、main.js は `#` 始まりの href だけを監視対象にしている
- 1180px以下でも横スクロールが出ないことを確認済み

## 10. ローカル確認

```bash
npx http-server . -p 4173 -c-1
```
→ http://127.0.0.1:4173/

## 11. 今後の相談事項

- マリーの追加ポーズ（設計シートにある「みつけた！」のカメラ姿、「いってきます」の後ろ姿など）を
  透過PNGで用意できれば、セクションごとに表情を変えられる
- 慶元寺・喜多見フローラル・横丁の新規撮影（人が写っているカット）
- 喜多見フローラル専用LPができたら SERVICE 04 からリンク
- PROJECTカードの数値（滞在時間・経済波及）の更新
- 問い合わせフォームの本送信化（Formspree等）
- JOURNAL（note）の記事一覧をサイト内に取り込むか
